# AGENTS

## Form Template Compatibility

This app is deployed on a legacy self-hosted Anvil server that does not support
modern HTML Form templates.

- New Forms must use legacy `form_template.yaml` templates.
- Do not create or convert Forms to `form_template.html`.
- Existing YAML Form templates must remain in YAML format.
- A Form must have exactly one template file.
